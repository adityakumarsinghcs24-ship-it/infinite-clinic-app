{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn project.wsgi:application --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/api/mongo/dashboard/stats/"
  }
}